---
import QuoteCTA from "@components/ui/QuoteCTA.astro";

interface Props {
  whatsappNumber: string;
}

const { whatsappNumber } = Astro.props;
---

<div class="od-cta">
  <div class="od-cta__inner">
    <QuoteCTA whatsappNumber={whatsappNumber} />
  </div>
</div>
