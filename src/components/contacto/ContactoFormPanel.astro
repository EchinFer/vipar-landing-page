---
import ContactFormGeneric from "@components/ContactFormGeneric.astro";
import { telephone } from "src/constants";

const whatsappNumber = telephone.replace(/[^\d]/g, "");
---

<div data-consent-sensitive>
  <ContactFormGeneric
    formId="vipar-contact-form"
    source="vipar-contacto"
    variant="contact"
    title="Cotiza tu proyecto y segui por WhatsApp con el contexto cargado"
    intro="Elegi el servicio, el tipo de consulta y deja tu correo. Usamos el mismo embudo que en home para responder mas rapido."
    showServiceSelect={true}
    showAsuntoSelect={true}
    showFileUpload={true}
    cardClass="contact-form-panel"
    formClass="contact-form"
    whatsappNumber={whatsappNumber}
  />
</div>
