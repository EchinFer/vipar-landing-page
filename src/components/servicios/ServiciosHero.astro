---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import Heading from "../ui/Heading.astro";
import Text from "../ui/Text.astro";

interface Props {
  image: ImageMetadata;
}

const { image } = Astro.props;
---

<section class="vipar-servicios-hero">
  <div class="vipar-servicios-hero__copy">
    <Heading level={1} variant="display">
      Cristales templados, aluminio y terminaciones para obras en Paraguay.
    </Heading>
    <Text size="lg">
      DiseÃ±o, provisiÃ³n e instalaciÃ³n de sistemas en vidrio templado, carpinterÃ­a de aluminio
      y terminaciones tÃ©cnicas para proyectos residenciales, comerciales y corporativos.
    </Text>
  </div>
  <div class="vipar-servicios-hero__media">
    <Image
      src={image}
      alt="Fachada arquitectÃ³nica"
      widths={[640, 960, 1280, 1600]}
      sizes="(max-width: 700px) 100vw, 50vw"
      loading="eager"
      fetchpriority="high"
      decoding="async"
      format="webp"
      quality={66}
    />
  </div>
</section>
