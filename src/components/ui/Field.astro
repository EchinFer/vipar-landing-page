---
interface Props {
  as?: string;
  class?: string;
}

const { as = "div", class: className = "" } = Astro.props;
const Tag = as as any;
---
<Tag class:list={["ds-field", className]}><slot /></Tag>
