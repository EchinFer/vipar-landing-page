---
interface Props extends astroHTML.JSX.TextareaHTMLAttributes {
  class?: string;
}

const { class: className = "", ...props } = Astro.props;
---
<textarea {...props} class:list={["ds-textarea", className]}></textarea>
