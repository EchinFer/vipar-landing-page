---
interface Props extends astroHTML.JSX.SelectHTMLAttributes {
  class?: string;
}

const { class: className = "", ...props } = Astro.props;
---
<select {...props} class:list={["ds-select", className]}><slot /></select>
