---
import FilterBar from "../ui/FilterBar.astro";
import type { FilterBarItem } from "@types";

interface Props {
  filters: FilterBarItem[];
}

const { filters } = Astro.props;
---

<FilterBar
  filters={filters}
  chipClass="vipar-obras-filter"
  class="vipar-obras-filter-bar"
/>
